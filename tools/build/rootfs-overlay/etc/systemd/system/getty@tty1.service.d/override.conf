# Delay getty on tty1 when the display manager is running.
# The compositor takes over the framebuffer, so getty on tty1
# would fight for the display. Getty still runs on serial console.
[Unit]
ConditionPathExists=!/run/user/0/wayland-0
